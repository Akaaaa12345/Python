중간점검

310p

1\. 포함된다

2\.인덱싱 , 슬라이딩 , 덧셈연산 ,곱셈연산



316p

1\.리스트는 수정 삭제 가능 튜블은 불가능

2.tuple()

3\.패킹 여러개의 값을 하나의 변수에 묶어서 담음 언패킹 묶여 있는 값을 다시 가가의 변수로 풀어냄

4\.반복 가능한 객체 를 바당서, 각 요소에 대해 형태의 튜플을 반환



322p

1\. 리스트는 순서 유지 되고 중복 가능하지만 세트는 순서가 유지안되고 중복 불가능.

2\. 사용 못한다.

3.A.intersection(B)

4.add()



331p

1.empty\_dict = {}

2.d\[k] = v

3.for key in d:

&#x20;   print(key)





342p



1.ord()

2\. -1 인덱스 사용

3\. 관계 연산자를 사용



349p

1.strip()

2.join()

3.split()



도전문제

325p

1\.

txt = input("학생 리포트 내용을 입력하세요: ")



words = txt.split(" ")

total\_word\_count = len(words)



unique = set(words)

unique\_word\_count = len(unique)



if total\_word\_count > 0:

&#x20;   ratio = (unique\_word\_count / total\_word\_count) \* 100

else:

&#x20;   ratio = 0



print("-" \* 30)

print(f"전체 단어 개수: {total\_word\_count}")

print(f"중복 제거 후 단어 개수: {unique\_word\_count}")

print(f"어휘 다양성 비율: {ratio:.2f}%")



if ratio >= 50:

&#x20;   print("최종 학점: A")

else:

&#x20;   print("최종 학점: B")

print("-" \* 30)



324p



s1 = input("첫 번째 문자열: ")

s2 = input("두 번째 문자열: ")



common\_chars = set(s1) \& set(s2)

common\_list = list(common\_chars)



total\_len = max(len(s1), len(s2))

common\_count = len(common\_list)



if total\_len > 0:

&#x20;   similarity = (common\_count / total\_len) \* 100

else:

&#x20;   similarity = 0



print("\\n공통적인 글자:", end=" ")

for i in common\_list:

&#x20;   print(i, end=" ")



print(f"\\n공통 문자 비율: {similarity:.2f}%")



if similarity >= 70:

&#x20;   print("결과: 표절 의심")

else:

&#x20;   print("결과: 정상")

&#x20;   print("결과: 정상")



332p

1.kor\_eng\_dict = {}



kor\_eng\_dict\["하나"] = "one"

kor\_eng\_dict\["둘"] = "two"

kor\_eng\_dict\["셋"] = "three"



word = input("한국어 단어를 입력하시오: ")

print(kor\_eng\_dict\[word])



2\.

english\_dict = {}



while True:

&#x20;   print("\\n1. 단어 검색 / 2. 단어 추가 / 3. 종료")

&#x20;   menu = input("메뉴를 선택하세요: ")



&#x20;   if menu == "1":

&#x20;       word = input("검색할 영어 단어: ")

&#x20;       if word in english\_dict:

&#x20;           print(f"뜻: {english\_dict\[word]}")

&#x20;       else:

&#x20;           print("사전에 없습니다.")

&#x20;           

&#x20;   elif menu == "2":

&#x20;       eng = input("추가할 영어 단어: ")

&#x20;       kor = input("단어의 뜻: ")

&#x20;       english\_dict\[eng] = kor

&#x20;       print("추가 완료!")

&#x20;       

&#x20;   elif menu == "3":

&#x20;       break

333p



def get\_contact():

&#x20;   name = input("이름: ")

&#x20;   number = input("전화번호: ")

&#x20;   return name, number



def display\_menu():

&#x20;   print("1. 연락처 추가")

&#x20;   print("2. 연락처 삭제")

&#x20;   print("3. 연락처 검색")

&#x20;   print("4. 연락처 출력")

&#x20;   print("5. 종료")

&#x20;   select = int(input("메뉴 항목을 선택하시오: "))

&#x20;   return select



def main():

&#x20;   address\_book = {}

&#x20;   while True:

&#x20;       user = display\_menu()

&#x20;       if user == 1:

&#x20;           name, number = get\_contact()

&#x20;           address\_book\[name] = number

&#x20;       elif user == 2:

&#x20;           name = input("삭제할 이름: ")

&#x20;           if name in address\_book:

&#x20;               address\_book.pop(name)

&#x20;               print(f"{name} 님이 삭제되었습니다.")

&#x20;           else:

&#x20;               print("해당 이름이 연락처에 없습니다.")

&#x20;       elif user == 3:

&#x20;           name = input("검색할 이름: ")

&#x20;           if name in address\_book:

&#x20;               print(f"{name} 의 전화번호: {address\_book\[name]}")

&#x20;           else:

&#x20;               print("검색 결과가 없습니다.")

&#x20;       elif user == 4:

&#x20;           for key in sorted(address\_book):

&#x20;               print(key, "의 전화번호:", address\_book\[key])

&#x20;       elif user == 5:

&#x20;           print("프로그램을 종료합니다.")

&#x20;           break

&#x20;       else:

&#x20;           print("잘못된 입력입니다.")



main()



335p

score\_dic = {}



while True:

&#x20;   name = input("학생 이름을 입력하세요 (종료하려면 'q' 입력): ")

&#x20;   if name.lower() == 'q':

&#x20;       break

&#x20;   

&#x20;   score1 = int(input("첫 번째 과목 성적: "))

&#x20;   score2 = int(input("두 번째 과목 성적: "))

&#x20;   score3 = int(input("세 번째 과목 성적: "))

&#x20;   

&#x20;   score\_dic\[name] = \[score1, score2, score3]



print("\\n--- 성적 결과 출력 ---")

for name, scores in score\_dic.items():

&#x20;   average = sum(scores) / len(scores)

&#x20;   print(f"{name}의 평균성적 = {average:.2f}")



343p

s = input("문자열을 입력하시오: ")



is\_palindrome = True

n = len(s)



for i in range(n // 2):

&#x20;   if s\[i] != s\[n - 1 - i]:

&#x20;       is\_palindrome = False

&#x20;       break



if is\_palindrome:

&#x20;   print("회문입니다.")

else:

&#x20;   print("회문이 아닙니다.")



349p

phrase = input("문자열을 입력하시오: ")

acronym = ""



stopwords = \["BY", "IN", "THE", "OF"]



for word in phrase.upper().split():

&#x20;   if word not in stopwords:

&#x20;       acronym += word\[0]



print(acronym)



350p

address = input("이메일 주소를 입력하시오: ")

parts = address.split(".")



print(address)

for part in parts:

&#x20;   print(part)



351p

sentence = input("문자열을 입력하시오: ")



table = {"alphas":0, "digits":0, "spaces":0, "uppers":0}



for i in sentence:

&#x20;   if i.isalpha():

&#x20;       table\["alphas"] += 1

&#x20;       if i.isupper():

&#x20;           table\["uppers"] += 1

&#x20;   if i.isdigit():

&#x20;       table\["digits"] += 1

&#x20;   if i.isspace():

&#x20;       table\["spaces"] += 1



print(table)

print(f"대문자 개수: {table\['uppers']}")



352p

t = input("트위터 메시지를 입력하시오: ")

words = t.split(" ")



forbidden\_word = "shit"

count = 0

clean\_words = \[]



for word in words:

&#x20;   if word.lower() == forbidden\_word:

&#x20;       count += 1

&#x20;   else:

&#x20;       clean\_words.append(word)



result = " ".join(clean\_words)



print(f"수정된 메시지: {result}")

print(f"금지된 단어 사용 횟수: {count}")



2\.

t = input("트위터 메시지를 입력하시오: ")

words = t.split(" ")



hashtag\_count = 0



for word in words:

&#x20;   if word.startswith("#"):

&#x20;       hashtag\_count += 1



print(f"해시태그 개수: {hashtag\_count}")



3\.

sentence = input("문자열을 입력하시오: ")



lower\_sentence = sentence.lower()

clean\_sentence = ""



punctuations = ".,!?;:"



for char in lower\_sentence:

&#x20;   if char not in punctuations:

&#x20;       clean\_sentence += char



print(clean\_sentence)



summary

튜플 , 딕셔너리 , 세트



exercise

1\.리스트나 딕셔너리를 사용한다. 세트는 중복이 허용이 안되므로 좋지 않다. 



2.Milk



3.500

(300, 400)



4\. print(aTuple\[::-1])



5\. mytuple = (10, 20, 30, 40)

a, b, c, d = mytuple



6\. b , d



7\. c



8\. mydict\["emp3"]\["salary"] = 8500



9\. student = { "name": "Kim", "age": "21", "score": 89 }

del student\["score"]



10\. a , b



11.a , c , d



12\. True



13\. {'Milk', 'Orange', 'Bread', 'Butter'}



14\. a , c



15\. True

True



16\. 문법 오류



17\. a,b



18.(b) ord("c")



19.Hel World



20\. {'r', 'd', 'm'}



프로그래밍.

1\.

def unique\_sort(lst):

&#x20; return sorted(set(lst))



lst = \[80, 20, 20, 30, 60, 30]

print("주어진 리스트", lst)

newLst = unique\_sort(lst)

print("정렬된 리스트", newLst)



2\. result = {x: x\*x for x in range(1, 11)}

print(result)



3\.

d = {"Apple": 1, "Banana": 2, "Grape": 3} 

for key, value in d.items():

&#x20;    print(key, "-> ", d\[key])



4\.

d = {1: 10, 2: 20, 3: 30, 4: 40, 5: 50, 6: 60}

key = int(input("키를 입력하시오: "))

if key in d:

&#x20;   print(f"키 {key}은 딕셔너리에 있습니다.")

else:

&#x20;   print(f"키 {key}은 딕셔너리에 없습니다.")



5\.

myDict = {"옷": 100, "컴퓨터": 2000, "모니터": 320}

print("총합계=", sum(myDict.values()))



6\.

colors = \["red", "green", "blue"]

values = \["#FF0000", "#008000", "#0000FF"]

color\_dict = dict(zip(colors, values))

print(color\_dict)



7\.

mydict = {}



for i in range(2):

&#x20;   date = input("날짜를 입력하시오: ")

&#x20;   job = input("일정을 입력하시오: ")



&#x20;   if date in mydict:

&#x20;       mydict\[date].append(job)

&#x20;   else:

&#x20;       mydict\[date] = \[]

&#x20;       mydict\[date].append(job)



print(mydict)



8\.

months = {1: "January", 2: "February", 3: "March", 4: "April", 5: "May", 6: "June",

&#x20;         7: "July", 8: "August", 9: "September", 10: "October", 11: "November", 12: "December"}

num = int(input("달의 번호: "))

print(f"달의 이름: {months.get(num)}")



9\.

s1 = input("첫 번째 문자열: ")

s2 = input("두 번째 문자열: ")



set1=set(s1)

set2=set(s2)



print("모두 포함된 글자: ", set1 \& set2)



10\.



set1 = {10, 20, 30, 40, 50, 60}

set2 = {30, 40, 50, 60, 70, 80}

result = set1 ^ set2

print(f"어느 한쪽에만 있는 요소들 {result}")



11\.



problems  = {"파이썬": "최근에 가장 떠오르는 프로그래밍 언어",

&#x20;               "변수": "데이터를 저장하는 메모리 공간",

&#x20;               "함수": "작업을 수행하는 문장들의 집합에 이름을 붙인것",

&#x20;               "리스트": "서로 관련이 없는 항목들의 모임",

&#x20;               }



def show\_words(problems):

&#x20;   display\_message = ""

&#x20;   i=1

&#x20;   for word in problems.keys():

&#x20;       display\_message += "("+str(i)+")"

&#x20;       display\_message += word + " "

&#x20;       i+=1

&#x20;   print( display\_message)



for meaning in problems.values():

&#x20;   print("다음은 어떤 단어에 대한 설명일까요? ")

&#x20;   print("\\""+meaning+"\\"")

&#x20;   correct = False

&#x20;   while not correct:

&#x20;       show\_words(problems)

&#x20;       guessed\_word = input("")    

&#x20;       if problems\[guessed\_word] == meaning:

&#x20;           print("정답입니다. !")

&#x20;           correct = True

&#x20;       else:

&#x20;           print("정답이 아닙니다.")



12\.

text = input("문자열을 입력하시오: ")

forbidden = input("금칙어를 입력하시오: ").split()

for word in forbidden:

&#x20;   text = text.replace(word, "\*" \* len(word))

print(text)



13\.

def count\_all(txt):

&#x20; return {

&#x20;   "LETTERS": sum(1 for x in txt if x.isalpha()),

&#x20;   "DIGITS": sum(1 for x in txt if x.isnumeric()),

&#x20; }

print(count\_all("Hello World123"))



14\.

date\_str = input("날짜 입력(MM/DD/YYYY): ")

parts = date\_str.split("/")

result = parts\[2] + parts\[0] + parts\[1]

print(result)



15\.

studentList = {

"Park": "Korea",

"Sam": "USA",

"Sakura": "Japan"

}

def greeting(name):

&#x20;   if name in studentList:

&#x20;       return 'Hi! I"m ' + name+ ', and I"m from ' + studentList\[name] + "."

&#x20;   else:

&#x20;       return 'Hi! I"m a guest.'



print(greeting("Park"))

print(greeting("Sam"))

print(greeting("Sakura"))



16\.

import random

s = "abcdefghijklmnopqrstuvwxyz0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ!@#$%^\&\*()"

password = "".join(random.sample(s, 8))

print(f"생성된 암호 = {password}")





















